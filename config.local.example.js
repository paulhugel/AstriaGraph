// Local (untracked) override for AstriaGraph config.
// Copy this file to `config.local.js` and customize as needed.
// This file is ignored by git (.gitignore) and will override values
// set in `config.js` when both are present.

// Example: point to a live API base (must allow CORS):
// window.ASTRIAGRAPH_API_BASE = "https://your-api-host/AstriaGraph/api";

