// Optional configuration for AstriaGraph
//
// To use a live API instead of static TSV files, set the base URL here.
// The API must enable CORS for your site origin.
//
// Example:
// window.ASTRIAGRAPH_API_BASE = "https://your-api-host/AstriaGraph/api";
//
// Leave this file as-is (no assignment) to use the built-in static
// data under assets/data/ when running on GitHub Pages.

